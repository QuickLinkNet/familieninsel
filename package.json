{
  "name": "familieninsel",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "node scripts/build.mjs",
    "test": "npm run test --workspace=frontend && npm run test:backend",
    "test:backend": "php backend/vendor/bin/phpunit -c backend/phpunit.xml",
    "lint:php": "node scripts/lint-php.mjs",
    "predeploy": "npm run build",
    "deploy": "node scripts/deploy.mjs"
  },
  "devDependencies": {
    "basic-ftp": "^5.0.5"
  }
}
