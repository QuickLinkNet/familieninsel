# Storage darf niemals oeffentlich erreichbar sein (SQLite-Datei, Logs, Backups).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Deny from all
</IfModule>
